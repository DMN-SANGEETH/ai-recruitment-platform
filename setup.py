from setuptools import setup, find_packages

setup(
    name="ai_recruitment_platform",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        "streamlit",
        # other dependencies
    ],
)